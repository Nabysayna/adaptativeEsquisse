import { FiltrervoperateursPipe } from './filtrervoperateurs.pipe';

describe('FiltrervoperateursPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrervoperateursPipe();
    expect(pipe).toBeTruthy();
  });
});
