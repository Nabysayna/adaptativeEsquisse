import { FiltrerevenusPipe } from './filtrerevenus.pipe';

describe('FiltrerevenusPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrerevenusPipe();
    expect(pipe).toBeTruthy();
  });
});
