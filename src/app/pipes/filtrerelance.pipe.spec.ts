import { FiltrerelancePipe } from './filtrerelance.pipe';

describe('FiltrerelancePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrerelancePipe();
    expect(pipe).toBeTruthy();
  });
});
