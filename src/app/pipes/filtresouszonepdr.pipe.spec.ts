import { FiltresouszonepdrPipe } from './filtresouszonepdr.pipe';

describe('FiltresouszonepdrPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltresouszonepdrPipe();
    expect(pipe).toBeTruthy();
  });
});
