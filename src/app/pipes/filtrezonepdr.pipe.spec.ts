import { FiltrezonepdrPipe } from './filtrezonepdr.pipe';

describe('FiltrezonepdrPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrezonepdrPipe();
    expect(pipe).toBeTruthy();
  });
});
