import { FiltregestionreportingPipe } from './filtregestionreporting.pipe';

describe('FiltregestionreportingPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltregestionreportingPipe();
    expect(pipe).toBeTruthy();
  });
});
