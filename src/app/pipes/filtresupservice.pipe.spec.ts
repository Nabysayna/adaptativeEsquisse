import { FiltresupservicePipe } from './filtresupservice.pipe';

describe('FiltresupservicePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltresupservicePipe();
    expect(pipe).toBeTruthy();
  });
});
