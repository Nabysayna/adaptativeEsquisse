import { CarddatafilterPipe } from './carddatafilter.pipe';

describe('CarddatafilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CarddatafilterPipe();
    expect(pipe).toBeTruthy();
  });
});
