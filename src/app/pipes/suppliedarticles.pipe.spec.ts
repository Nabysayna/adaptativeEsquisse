import { SuppliedarticlesPipe } from './suppliedarticles.pipe';

describe('SuppliedarticlesPipe', () => {
  it('create an instance', () => {
    const pipe = new SuppliedarticlesPipe();
    expect(pipe).toBeTruthy();
  });
});
