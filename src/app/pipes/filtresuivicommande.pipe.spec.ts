import { FiltresuivicommandePipe } from './filtresuivicommande.pipe';

describe('FiltresuivicommandePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltresuivicommandePipe();
    expect(pipe).toBeTruthy();
  });
});
