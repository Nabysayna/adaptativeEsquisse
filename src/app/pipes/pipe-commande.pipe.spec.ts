import { PipeCommandePipe } from './pipe-commande.pipe';

describe('PipeCommandePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeCommandePipe();
    expect(pipe).toBeTruthy();
  });
});
