import { FiltrerecommandealivrerPipe } from './filtrerecommandealivrer.pipe';

describe('FiltrerecommandealivrerPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrerecommandealivrerPipe();
    expect(pipe).toBeTruthy();
  });
});
