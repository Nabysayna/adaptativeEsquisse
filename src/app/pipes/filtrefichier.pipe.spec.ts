import { FiltrefichierPipe } from './filtrefichier.pipe';

describe('FiltrefichierPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrefichierPipe();
    expect(pipe).toBeTruthy();
  });
});
