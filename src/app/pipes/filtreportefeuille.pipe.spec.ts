import { FiltreportefeuillePipe } from './filtreportefeuille.pipe';

describe('FiltreportefeuillePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreportefeuillePipe();
    expect(pipe).toBeTruthy();
  });
});
