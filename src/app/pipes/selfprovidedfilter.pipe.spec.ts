import { SelfprovidedfilterPipe } from './selfprovidedfilter.pipe';

describe('SelfprovidedfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SelfprovidedfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
