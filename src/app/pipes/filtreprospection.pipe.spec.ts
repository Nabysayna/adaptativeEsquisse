import { FiltreprospectionPipe } from './filtreprospection.pipe';

describe('FiltreprospectionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreprospectionPipe();
    expect(pipe).toBeTruthy();
  });
});
