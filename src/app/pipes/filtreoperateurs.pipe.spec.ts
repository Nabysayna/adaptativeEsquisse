import { FiltreoperateursPipe } from './filtreoperateurs.pipe';

describe('FiltreoperateursPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreoperateursPipe();
    expect(pipe).toBeTruthy();
  });
});
