import { DecodatafilterPipe } from './decodatafilter.pipe';

describe('DecodatafilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DecodatafilterPipe();
    expect(pipe).toBeTruthy();
  });
});
